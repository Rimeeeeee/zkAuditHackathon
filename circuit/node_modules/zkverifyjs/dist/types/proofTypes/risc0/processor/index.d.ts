import { ProofProcessor } from '../../../types';
import { Risc0Vk, Risc0Pubs } from '../types';
import { ProofOptions } from '../../../session/types';
declare class Risc0Processor implements ProofProcessor {
    formatProof(proof: string, options: ProofOptions, version: string): Record<string, string>;
    formatVk(vk: Risc0Vk['vk']): string;
    formatPubs(pubs: Risc0Pubs['pubs']): string;
}
declare const _default: Risc0Processor;
export default _default;
//# sourceMappingURL=index.d.ts.map