export async function accountInfo(api, account) {
    const { data: { free, reserved }, nonce, } = await api.query.system.account(account.address);
    return {
        address: account.address,
        nonce: nonce.toNumber(),
        freeBalance: free.toString(),
        reservedBalance: reserved.toString(),
    };
}
